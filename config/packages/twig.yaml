twig:
    paths: ['%kernel.project_dir%/templates']
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    form_themes: ['form_fields.html.twig']
    date:
        format: !php/const DateTimeInterface::W3C
        timezone: UTC
    globals:
        google_recaptcha_key: '%env(GOOGLE_RECAPTCHA_KEY)%'
