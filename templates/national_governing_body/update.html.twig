{% extends 'admin_base.html.twig' %}

{% block content %}
    <h1>{{ ngb.name }}</h1>

    {{ form_start(form) }}

    {{ form_rest(form) }}

    <div class="btn-group" role="group">
        <button type="submit" class="btn btn-primary">Update</button>
        <a href="{{ path('app_nationalgoverningbody_getlist') }}" class="btn btn-outline-secondary">Back</a>
    </div>

    {{ form_end(form) }}
{% endblock %}
