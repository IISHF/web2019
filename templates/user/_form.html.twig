{{ form_start(form) }}

{{ form_rest(form) }}

<div class="btn-group" role="group">
    <button type="submit" class="btn btn-primary">{{ submitBtnText|default('Update') }}</button>
    <a href="{{ path('app_user_list') }}" class="btn btn-outline-secondary">Back</a>
</div>

{{ form_end(form) }}